# Build artifacts
/build/
/rust/target/
OpenHomeNX.elf
OpenHomeNX.nro
OpenHomeNX.nacp
*.o
*.map

# OS / editor
.DS_Store

# Documentazione di lavoro interna (mai pubblica: riferimenti ad agenti/AI)
AGENTS.md
/docs/

# Runtime / logs / scratch
debug.log
__pycache__/
*.pyc
node_modules/
package.json
package-lock.json

# vendored reference clone (not built) — re-clone from github.com/Insektaure/pkHouse
/pkHouse/

# vendored USB FS lib clone (FAT-only ISC build in ./libusbhsfs/lib) — its own git repo
/libusbhsfs/

# release artifacts
/dist/
include/app_version.h
update.cfg


# personal save dumps (OTveri) — local only; upstream fixtures stay tracked
/tools/test save/*
!/tools/test save/upstream/
